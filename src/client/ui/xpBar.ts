// Barra de experiencia y nivel del HUD (encima de la barra rápida). PENDIENTE (tanda de experiencia).
import type { Experience } from '../game/experience';

export function renderXpBar(xp: Experience, show: boolean): void {
  void xp;
  void show;
}
